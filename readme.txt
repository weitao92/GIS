This is a GIS database which use several ways to index, sort and search GIS data. 

Using hashtable to index and search data based on name.
Using prQuadTree to index and search data based on coordinate.
Using AVLtree to index and search data based on FID.
